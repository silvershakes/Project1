import { OpenArticleDirective } from './open-article.directive';

describe('OpenArticleDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenArticleDirective();
    expect(directive).toBeTruthy();
  });
});
